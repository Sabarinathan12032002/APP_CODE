# APP_CODE
A graphical desktop application (floating screenshot toolbar) built with JavaFX and Swing, using Apache POI to write Word documents. The app captures screenshots and generates Word documents with the captured content. It's designed as a Windows installer that bundles Java runtime, so users don't need Java pre-installed.
